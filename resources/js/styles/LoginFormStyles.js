/**
 * External dependencies.
 */
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    default: {

    },
    title: {
        color: '#E0E4CC',
        fontSize: 48,
        textAlign: 'center',
        fontWeight: 'bold',
    },
});
